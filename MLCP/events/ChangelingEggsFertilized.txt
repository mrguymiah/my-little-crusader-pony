namespace = ChangelingEggsFert
character_event = {
	id = ChangelingEggsFert.1
	title = "changeling.eggsFertile.Title"
	desc = "changeling.eggsFertile.Desc"
	trigger = {
		has_character_flag = laid_eggs
	}
	option = {
		name = "changeling.eggsFertile.confirmed" # Excellent!
		add_trait = "changeling_eggs"
		character_event = { id = ChangelingEggsHatch.1 days = 365}
		clr_character_flag = laid_eggs
		any_vassal = {
			limit = { trait = changeling }
			opinion = {
				who = ROOT
				modifier = changeling_liege_has_eggs #Needs localization
				years = 1
			}
		}
	}
}

character_event = {
	id = ChangelingEggsFert.2
	title = "changeling.liegeEggsFertile.Title" #Liege has fertilized eggs! #Needs localization
	desc = "changeling.liegeEggsFertile.Desc" #Needs localization

	trigger = {
		trait = changeling
		liege = {
			trait = changeling_eggs
		}
		NOT = {
			has_opinion_modifier = {
				who = liege
				modifier = changeling_liege_has_eggs
			}
		}
	}

	option = {
		name = "changeling.liegeEggsFertile.confirmed" # Excellent!
		opinion = {
			who = liege
			modifier = changeling_liege_has_eggs
			years = 1
		}
	}
}
